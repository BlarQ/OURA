import { LoginView } from '@/components/auth/LoginView';

export const metadata = {
  title: 'Sign In | OURA',
  description: 'Sign in to your OURA Personal OS workspace.',
};

export default function LoginPage() {
  return <LoginView />;
}
