import { SignupView } from '@/components/auth/SignupView';

export const metadata = {
  title: 'Step-by-Step Signup & Setup | OURA',
  description: 'Create your account and complete step-by-step onboarding for OURA.',
};

export default function SignupPage() {
  return <SignupView />;
}
