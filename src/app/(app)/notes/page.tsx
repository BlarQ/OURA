import { NotesView } from '@/components/notes/NotesView';

export const metadata = {
  title: 'Notepad & Notes | OURA',
  description: 'Mobile notepad app for storing thoughts, editing notes, and sharing as messages.',
};

export default function NotesPage() {
  return <NotesView />;
}
